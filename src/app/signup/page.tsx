import SignupPage from '@/app/[lang]/signup/page';

// This is a workaround to make sure the signup page is available at /signup
// while also being compatible with i18n routing.
export default SignupPage;
